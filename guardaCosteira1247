#include <bits/stdc++.h>
#include <cmath>

using namespace std;
int main(){
    float D,VF, VG;
    while(cin >>D >> VF >> VG){
        float distanciaG,tempoG,tempoF;
        distanciaG = (sqrt(144+(pow(D,2))));
        tempoG = (distanciaG/VG);
        tempoF = (12/VF);
        if(tempoG<=tempoF){
            cout<<'S'<<endl;
        }
        else{
            cout<<'N'<<endl;
        }
    }
    return 0;
}
